# Patch 0.31.5

User authorized merging PR #64 and releasing a patch. Release implicit continuation by default with opt-in execution contracts as v0.31.5. Preserve the approved implementation and publish through the existing trusted-publishing workflow.
