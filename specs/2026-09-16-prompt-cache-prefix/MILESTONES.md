# Implementation log

- Read issue #67 and audited context injection, before_agent_start, checkpoint normalization, tool-profile selection, drafting, and isolated auditor/Oracle sessions. Used a clean worktree from main because the user's original checkout contains unrelated budget/recovery edits.
- Found three prefix invalidators: prepended mutable state, per-turn dynamic system suffixes, and removal of prior checkpoints. Consolidated lifecycle rendering at the request-only tail, preserving preflight aborts/accounting and current status, budget, task, rejection and resync behavior. Removed obsolete scheduler flags that distinguished live-context eligibility between normal/custom runs.
- Explicit-cache review found that merely moving the live block leaves Pi's write breakpoint after transient content that will not recur. Relocated existing Anthropic/compatible and Bedrock breakpoints onto reusable history, preserving retention settings and marker count. Implicit caching is untouched.
- Confirmed execution tool installation already avoids schema churn across focus/status/progress. Drafting transitions deliberately change tools. Auditor/Oracle default isolated loaders have fixed policies/profiles and append their goal/evidence requests; no cache-setting override is warranted.
- Updated composed-context measurement to include request-only state and retain bounded historical markers. The get_goal fixture previously appended tool results *after* context transformation; corrected its ordering to reflect the actual provider pipeline. Regenerated baseline with this rationale: state moved from system to messages and every checkpoint retains a tiny stable marker.
- Initial lifecycle regression failure asserted old systemPrompt output; migrated it to the context tail. A new fixture assumed scheduling state was initialized; fixed it to construct valid state explicitly. Actual provider tests initially passed standalone but failed under the fast runner's SDK stubs; moved serialization checks into an isolated SDK worker using the repository's existing e2e pattern.
- Validation: typecheck and lint passed; all 1,007 tests passed across 79 test files; 35 targeted cache/scheduler checks passed after the provider-hook assertion; all 24 context fixtures passed; six real SDK parent/auditor/Oracle payload captures passed. The separate SDK worker checks four serializers with short/long/none retention. No paid provider requests were made; these checks do not measure live cache-hit rates.

- Release follow-up: the user authorized merging PR #68 and publishing patch 0.31.6. PR #68 merged as 32bae00. External A/B verification on commit c23004b reports approximately 97% warmed coverage and zero detected cache-miss turns on OpenCode/DeepSeek; this is provider-specific evidence, not a cross-provider guarantee. Source: https://github.com/tmonk/pi-goal-x/issues/67#issuecomment-5712607533.
